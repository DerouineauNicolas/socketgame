version: '3'
services:     
  back:
    build:
      context: .
      dockerfile: ./backend/Dockerfile.prod
    ports:
      - "8081:8081"
    volumes:
      - ./server/:/usr/src/app/
    command: npm start
